{% extends "RingCoinAppBundle:Registration:check_email.html.twig" %}

{% trans_default_domain 'FOSUserBundle' %}

{% block title %}confirmed_registration{% endblock %}

{% block content %}
    {{ 'registration.confirmed'|trans({'%username%': user.username}) }}
    {% if targetUrl %}
        <p><a href="{{ targetUrl }}">{{ 'registration.back'|trans }}</a></p>
    {% endif %}
{% endblock %}

{% block link %}
    <a href="{{ path('fos_user_profile_show', {page:1}) }}">Backoffice <i class="fa fa-angle-double-right" aria-hidden="true"></i></a>
{% endblock %}